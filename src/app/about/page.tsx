import Trips from "@/app/about/components/Trips";

export const metadata = {
  title: "About",
};

const About = () => {
  return (
    <div>
      <Trips />
    </div>
  );
};

export default About;
